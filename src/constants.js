export const NAME_MAX = 24;
export const PROMPT_MAX = 300;
export const ANSWER_MAX = 120;
export const ATTEMPT_MAX = 200;
export const TIMER_OPTIONS = [0, 30, 60, 90, 120, 150, 180];
export const REACTIONS = ['👍', '😂', '😮', '🎉', '😢', '🔥'];

export const valuesForCount = (count) => Array.from({ length: count }, (_, i) => (i + 1) * 100);
